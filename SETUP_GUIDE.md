# 학원관리 시스템 새로 만들기 — 완전 초보자 가이드

이 문서는 지금 이 시스템(노션 데이터베이스 10개 + 웹 대시보드)과 **완전히 똑같은 것을 다른 학원용으로 처음부터 새로 만드는 방법**을 순서대로 설명합니다. 개발 지식이 전혀 없어도 따라 할 수 있도록, 화면에서 무엇을 클릭해야 하는지까지 최대한 자세히 적었습니다.

전체 과정은 대략 **1~2시간** 걸리고, 다음 4가지 계정이 필요합니다 (전부 무료로 시작 가능):

| 계정 | 용도 | 가입 링크 |
|---|---|---|
| Notion | 데이터를 저장하는 곳 (반/학생/출결/상담 등 모든 기록) | https://www.notion.so |
| GitHub | 프로그램 코드를 보관하는 곳 | https://github.com |
| Vercel | 웹사이트를 실제로 띄우는(배포하는) 곳 | https://vercel.com |
| Anthropic | 자연어 입력("이그잼 AI") 기능에 쓰는 AI | https://console.anthropic.com |

> 💡 GitHub와 Vercel은 가입할 때 "Continue with GitHub" 같은 버튼으로 서로 연결해두면 이후 단계가 훨씬 쉬워집니다.

---

## 전체 순서 요약

1. Notion에 데이터베이스 10개 만들기
2. Notion 연동(Integration) 만들어서 토큰 발급받기
3. 코드를 내 GitHub 계정으로 가져오기
4. Anthropic API 키 발급받기
5. Vercel에 배포하고 환경변수 12개 입력하기
6. 학원 이름(브랜드) 바꾸기
7. 직원 계정 / 반 / 학생 초기 데이터 입력하기
8. 로그인해서 테스트하기

---

## 1단계. Notion에 데이터베이스 10개 만들기

### 1-1. 최상위 페이지 만들기

1. Notion에 로그인 → 왼쪽 사이드바에서 **"+ 페이지 추가"** 클릭
2. 페이지 이름을 `학원관리` (원하는 학원 이름으로 바꿔도 됩니다)로 지정
3. 이 페이지 안에 아래 10개의 데이터베이스를 **인라인 데이터베이스**로 하나씩 만듭니다 (`/database` 입력 → "데이터베이스 - 인라인" 선택)

### 1-2. 데이터베이스 10개 스키마

아래 표대로 각 데이터베이스에 속성(컬럼)을 만드세요. **속성 이름과 타입은 반드시 표와 똑같이** 만들어야 합니다(코드가 이 이름들을 그대로 참조하기 때문입니다). Select/Multi-select의 옵션 값도 표에 적힌 그대로 만드세요 (순서/색상은 상관없음).

> 표기 설명: `title`=제목 속성(모든 DB에 기본 1개 있음) · `text`=텍스트 · `select`=단일 선택 · `multi_select`=다중 선택 · `date`=날짜 · `checkbox`=체크박스 · `number`=숫자 · `phone`=전화번호 · `relation`=관계형(다른 DB와 연결) · `rollup`=관계된 데이터 집계 · `formula`=수식

---

#### DB① 반

| 속성명 | 타입 | 옵션/연결 대상 |
|---|---|---|
| 반이름 | title | |
| 담당교사 | text | |
| 요일 | multi_select | 월, 화, 수, 목, 금, 토, 일 |
| 시간 | text | |
| 레벨 | select | 초등, 중등, 고등 |
| 소속학생 | relation | → DB② 학생마스터 |
| 진도기록 | relation | → DB③ 반별진도입력 |

> `학생일일기록`, `관련할일` 관계 속성은 DB④·DB⑱ 쪽에서 관계를 만들면 Notion이 자동으로 만들어줍니다. 직접 만들 필요 없습니다.

#### DB② 학생마스터

| 속성명 | 타입 | 옵션/연결 대상 |
|---|---|---|
| 이름 | title | |
| 소속반 | relation | → DB① 반 |
| 학년 | select | 초1, 초2, 초3, 초4, 초5, 초6, 중1, 중2, 중3, 고1, 고2, 고3 |
| 연락처 | phone | |
| 학부모연락처 | phone | |
| 등록일 | date | |
| 상태 | select | 재원, 휴원, 퇴원 |
| 학교 | text | |
| 등원일 | date | |
| 회비일 | number | |
| 학습레벨 | text | |
| 조치 | text | |
| 조치담당자 | text | |
| 조치알람일 | date | |

**추가로 만들어야 하는 롤업(Rollup) 3개** (DB④를 먼저 만든 뒤에 작업하세요):

| 속성명 | 타입 | 설정 |
|---|---|---|
| 누적출석률 | rollup | 관계=일일기록, 대상속성=DB④의 `출석점수`(수식), 계산=평균(average) |
| 누적숙제제출률 | rollup | 관계=일일기록, 대상속성=DB④의 `과제여부`(체크박스), 계산=체크된 비율(percent checked) |
| 누적단어테스트통과율 | rollup | 관계=일일기록, 대상속성=DB④의 `단어테스트점수`(수식), 계산=평균(average) |

#### DB③ 반별진도입력

| 속성명 | 타입 | 옵션/연결 대상 |
|---|---|---|
| 제목 | title | |
| 반 | relation | → DB① 반 |
| 날짜 | date | |
| 수업과목 | multi_select | 문법, 독해, 서술형, 구문, 듣기, 모의고사, 어법, 내신대비 |
| 진도내용 | text | |
| 과제내용 | text | |
| 다음시간테스트 | text | |
| 전달사항 | text | |
| 학생기록생성됨 | checkbox | |
| 생성된학생기록 | relation | → DB④ 학생별일일기록 |

#### DB④ 학생별일일기록

| 속성명 | 타입 | 옵션/연결 대상 |
|---|---|---|
| 제목 | title | |
| 학생 | relation | → DB② 학생마스터 |
| 반 | relation | → DB① 반 |
| 날짜 | date | |
| 진도내용 | text | |
| 출결 | select | 출석, 지각, 결석 |
| 출석점수 | formula | `if(prop("출결") == "결석", 0, 1)` |
| 과제여부 | checkbox | |
| 단어테스트결과 | select | 통과, 재시험, 미응시 |
| 단어테스트점수 | formula | `if(prop("단어테스트결과") == "통과", 1, 0)` |
| 성취사항 | text | |
| 비고 | text | |
| 반별진도원본 | relation | → DB③ 반별진도입력 |

> `출석점수`/`단어테스트점수`는 화면에는 안 보여도 되는 "숨김용 계산 컬럼"입니다. Select 값은 그대로 롤업(평균)할 수 없어서, 0/1 숫자로 바꿔주는 역할만 합니다. 만든 뒤 속성 메뉴에서 "숨기기" 해두면 깔끔합니다.

#### DB⑤ 데일리브리핑

| 속성명 | 타입 | 옵션/연결 대상 |
|---|---|---|
| 제목 | title | |
| 학생 | relation | → DB② 학생마스터 |
| 날짜 | date | |
| 브리핑유형 | select | 칭찬, 주의, 전달사항, 기타 |
| 브리핑내용 | text | |

#### DB⑦ 시험성적

| 속성명 | 타입 | 옵션/연결 대상 |
|---|---|---|
| 제목 | title | |
| 학생 | relation | → DB② 학생마스터 |
| 시험명 | text | |
| 과목 | select | 원하는 과목명 자유롭게 (예: 영어) |
| 날짜 | date | |
| 점수 | number | |

#### DB⑧ 상담일지

| 속성명 | 타입 | 옵션/연결 대상 |
|---|---|---|
| 제목 | title | |
| 학생 | relation | → DB② 학생마스터 |
| 날짜 | date | |
| 상담자 | text | |
| 상담내용 | text | |
| 전사내용 | text | |
| 후속조치 | text | |
| 입력자 | text | (직원이 상담일지를 실제로 입력한 사람 — 화면에서 자동으로 채워짐, 직접 입력 안 해도 됨) |

#### DB⑭ 행정입력함

| 속성명 | 타입 | 옵션/연결 대상 |
|---|---|---|
| 제목 | title | |
| 입력유형 | select | 결석예정, 긴급상담요청, 신규생문의, 기타 |
| 대상학생 | relation | → DB② 학생마스터 |
| 날짜 | date | |
| 종료일 | date | |
| 내용 | text | |
| 신규생정보 | text | |
| 처리완료 | checkbox | |
| 입력자 | text | (자동으로 채워짐) |

#### DB⑱ 할일관리

| 속성명 | 타입 | 옵션/연결 대상 |
|---|---|---|
| 제목 | title | |
| 유형 | select | 수업준비, 행정, 상담, 기타, 보강, 재시, 신입생상담, 레벨체크, 조치사항 |
| 관련학생 | relation | → DB② 학생마스터 |
| 관련반 | relation | → DB① 반 |
| 담당자 | relation | → DB⑲ 직원계정 |
| 시간 | text | |
| 예정일 | date | |
| 완료여부 | checkbox | |
| 우선순위 | select | 높음, 보통, 낮음 |

#### DB⑲ 직원계정

| 속성명 | 타입 | 옵션/연결 대상 |
|---|---|---|
| 이름 | title | |
| PIN | text | (로그인 비밀번호로 쓸 4자리 숫자, 예: 1234) |
| 역할 | select | 원장, 행정, 강사 |

> ⚠️ **주의**: `관계(relation)`를 만들 때, 아직 만들지 않은 DB를 연결해야 하는 경우(예: DB①의 `소속학생` → DB②가 아직 없을 때)가 있습니다. 이런 경우 DB②를 먼저 만들고 나서 DB①로 돌아와 관계를 추가하면 됩니다. 순서: DB②(최소한으로) → DB① → 나머지 순서대로 진행하는 것을 추천합니다.

---

## 2단계. Notion 연동(Integration) 만들고 토큰 받기

1. https://www.notion.so/my-integrations 접속
2. **"+ New integration"** 클릭
3. 이름을 아무렇게나 입력 (예: `학원관리봇`), 워크스페이스 선택 → **Submit**
4. 생성된 화면에서 **"Internal Integration Secret"** 값을 복사해둡니다. `ntn_`으로 시작하는 긴 문자열입니다. **이 값은 절대 다른 사람에게 공유하거나 GitHub에 올리면 안 됩니다.**
5. Notion으로 돌아가서, 1단계에서 만든 최상위 페이지(`학원관리`) 오른쪽 위 **"..."** 메뉴 → **"연결 추가"(Add connections)** → 방금 만든 연동 이름을 선택해서 연결합니다. 이렇게 하면 그 페이지 아래 있는 10개 데이터베이스 전부에 연동이 접근할 수 있게 됩니다.

## 3단계. 각 데이터베이스의 ID(데이터소스 ID) 확인하기

프로그램이 각 데이터베이스를 구분하려면 고유 ID가 필요합니다.

1. 각 데이터베이스를 열고, 오른쪽 위 **"..."** → **"페이지 링크 복사"**
2. 복사된 링크는 이런 형태입니다: `https://www.notion.so/워크스페이스/DB이름-abcdef1234567890abcdef1234567890?v=...`
3. `-` 뒤에 있는 32자리 문자열이 ID입니다 (`abcdef1234567890abcdef1234567890`). 이 값을 아래처럼 하이픈을 붙여 8-4-4-4-12 형태로 바꿔주면 최종 ID입니다: `abcdef12-3456-7890-abcd-ef1234567890`
4. 10개 데이터베이스 모두 이 방법으로 ID를 확인해서 메모장에 적어두세요.

---

## 4단계. 코드를 내 GitHub 계정으로 가져오기

1. GitHub에 로그인 → 오른쪽 위 **"+"** → **"New repository"**
2. 저장소 이름 입력 (예: `my-academy-dashboard`), Private(비공개) 선택 후 생성
3. 지금 이 프로젝트 폴더의 코드를 새 저장소에 올립니다. (기존 코드를 다운로드해서 새 저장소로 push 하거나, 이 프로젝트를 아는 개발자/AI 비서에게 "이 코드를 내 새 GitHub 저장소 `my-academy-dashboard`로 복사해줘"라고 요청하면 됩니다.)

> 💡 만약 Claude Code 같은 AI 코딩 비서를 쓸 수 있다면, "지금 프로젝트 코드 전체를 그대로 복사해서 새로 만든 내 GitHub 저장소 `(주소)`에 올려줘"라고 요청하는 것이 가장 쉽습니다.

---

## 5단계. Anthropic API 키 발급받기

1. https://console.anthropic.com 가입/로그인
2. 왼쪽 메뉴 **"Settings" → "API Keys"** → **"Create Key"**
3. 만들어진 키(`sk-ant-`로 시작) 복사해서 메모장에 저장 (이 화면을 벗어나면 다시 볼 수 없으니 꼭 저장하세요)
4. 결제 수단 등록이 필요할 수 있습니다 (사용량에 따라 소액 과금 — 학원 규모 기준 하루 수백 건 입력해도 월 몇 달러 수준입니다)

---

## 6단계. Vercel에 배포하기

1. https://vercel.com 접속 → GitHub 계정으로 로그인
2. **"Add New..." → "Project"**
3. 4단계에서 만든 GitHub 저장소(`my-academy-dashboard`)를 선택 → **"Import"**
4. 배포 설정 화면에서 **"Environment Variables"** 섹션을 펼치고, 아래 12개 값을 하나씩 입력합니다:

| 변수 이름 | 값 |
|---|---|
| `NOTION_TOKEN` | 2단계에서 복사한 `ntn_...` 값 |
| `NOTION_DB_CLASS` | DB① 반의 ID |
| `NOTION_DB_STUDENT` | DB② 학생마스터의 ID |
| `NOTION_DB_CLASS_PROGRESS` | DB③ 반별진도입력의 ID |
| `NOTION_DB_DAILY_RECORD` | DB④ 학생별일일기록의 ID |
| `NOTION_DB_BRIEFING` | DB⑤ 데일리브리핑의 ID |
| `NOTION_DB_EXAM_SCORE` | DB⑦ 시험성적의 ID |
| `NOTION_DB_COUNSELING` | DB⑧ 상담일지의 ID |
| `NOTION_DB_ADMIN_INBOX` | DB⑭ 행정입력함의 ID |
| `NOTION_DB_TODO` | DB⑱ 할일관리의 ID |
| `NOTION_DB_STAFF` | DB⑲ 직원계정의 ID |
| `SESSION_SECRET` | 아무 긴 임의의 문자열 (터미널이 있다면 `openssl rand -hex 32` 명령으로 생성 가능, 없으면 아무 키보드로 40자 이상 무작위로 입력해도 됩니다) |
| `ANTHROPIC_API_KEY` | 5단계에서 복사한 `sk-ant-...` 값 |

5. **"Deploy"** 클릭 → 1~2분 후 배포 완료. 완료되면 `https://프로젝트이름.vercel.app` 형태의 주소가 생성됩니다.

---

## 7단계. 학원 이름(브랜드) 바꾸기

코드 곳곳에 "이그잼영어학원"이 하드코딩되어 있어서, 다른 학원용으로 쓰려면 아래 5곳을 학원 이름으로 바꿔야 합니다. (GitHub 저장소에서 파일을 열어 직접 수정하거나, AI 코딩 비서에게 "이그잼영어학원을 전부 [새 학원 이름]으로 바꿔줘"라고 요청하세요.)

| 파일 | 내용 |
|---|---|
| `app/login/page.tsx` | 로그인 화면 제목 |
| `app/layout.tsx` | 브라우저 탭 제목 |
| `components/TopBar.tsx` | 상단 바 제목 |
| `lib/briefingFormat.ts` | 데일리브리핑 문자 템플릿 첫 문장 |
| `components/NaturalLanguageInput.tsx` | "이그잼 AI" 라는 자연어 입력창 이름 (원하는 이름으로) |

수정 후 GitHub에 커밋·푸시하면 Vercel이 자동으로 다시 배포합니다.

---

## 8단계. 초기 데이터 입력하기

배포된 사이트에 아직 로그인할 계정이 없으니, **Notion에서 직접** 아래 순서로 최소 데이터를 넣어주세요.

1. **DB⑲ 직원계정**에 원장님 본인 계정을 하나 추가: 이름, PIN(예: `1234`), 역할=`원장`
2. **DB① 반**에 실제 운영 중인 반을 몇 개 추가: 반이름, 담당교사, 요일, 시간, 레벨
3. **DB② 학생마스터**에 학생을 등록 (또는 사이트 배포 후 `/input` 페이지의 "학생정보수정" 화면에서 입력해도 됩니다)

> 💡 데이터가 많다면, `scripts/seed.mjs` 파일(샘플 데이터 자동 생성 스크립트)을 참고해서 비슷한 방식으로 대량 등록 스크립트를 만들 수도 있습니다. (개발 지식이 필요한 단계이니, 소규모라면 그냥 Notion 화면에서 직접 입력하는 것을 추천합니다.)

---

## 9단계. 로그인해서 테스트하기

1. Vercel에서 받은 주소(`https://프로젝트이름.vercel.app/login`)로 접속
2. 8단계에서 등록한 이름/PIN으로 로그인
3. `/dashboard`에서 화면이 정상적으로 뜨는지 확인 (처음에는 데이터가 없어서 대부분 "기록이 없습니다"로 나오는 게 정상입니다)
4. `/input`에서 반/학생을 하나 골라 수업 기록을 하나 입력해보고, 대시보드에 잘 반영되는지 확인

---

## 자주 발생하는 문제

**"로그인이 안 돼요"**
→ DB⑲ 직원계정의 이름/PIN 철자를 다시 확인하세요. PIN은 텍스트(text) 타입이라 앞자리가 0으로 시작해도 괜찮습니다.

**"대시보드에 아무것도 안 보여요"**
→ 정상입니다. 데이터를 입력하지 않으면 대부분의 화면은 "기록이 없습니다"라고만 표시됩니다. `/input`에서 데이터를 입력해보세요.

**"환경변수를 잘못 입력한 것 같아요"**
→ Vercel 프로젝트 화면 → **Settings → Environment Variables**에서 언제든 값을 수정할 수 있습니다. 수정 후에는 **Deployments** 탭에서 최신 배포를 "Redeploy" 해야 반영됩니다.

**"자연어 입력이 계속 실패해요"**
→ Anthropic 계정에 결제 수단이 등록되어 있는지, API 키가 정확히 복사됐는지 확인하세요.

**"노션 관계(relation)를 잘못 연결했어요"**
→ 데이터베이스 속성 위에 마우스를 올리고 편집(연필 아이콘)을 눌러 언제든 다시 연결 대상을 바꿀 수 있습니다.

---

## 부록. 환경변수 12개 한눈에 보기

```
NOTION_TOKEN=
NOTION_DB_CLASS=
NOTION_DB_STUDENT=
NOTION_DB_CLASS_PROGRESS=
NOTION_DB_DAILY_RECORD=
NOTION_DB_BRIEFING=
NOTION_DB_EXAM_SCORE=
NOTION_DB_COUNSELING=
NOTION_DB_ADMIN_INBOX=
NOTION_DB_TODO=
NOTION_DB_STAFF=
SESSION_SECRET=
ANTHROPIC_API_KEY=
```

로컬 컴퓨터에서 직접 실행해보고 싶다면, 프로젝트 폴더에 `.env.local` 파일을 만들어 위 값을 채운 뒤 터미널에서 `npm install` → `npm run dev`를 실행하면 `http://localhost:3000`에서 확인할 수 있습니다. (`.env.local.example` 파일에 예시 형식이 있습니다.)
